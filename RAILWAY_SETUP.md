# 🚂 Детальная инструкция по настройке Railway

## Добавление переменных окружения в Railway

### Способ 1: Через веб-интерфейс Railway

#### Шаг 1: Войдите в проект

1. Откройте [Railway Dashboard](https://railway.app)
2. Найдите ваш проект `botcs` в списке проектов
3. Кликните на название проекта, чтобы открыть его

#### Шаг 2: Откройте раздел Variables

У вас есть два варианта:

**Вариант A: Через меню проекта**
1. В верхней части страницы проекта найдите вкладки: "Deployments", "Metrics", "Variables", "Settings"
2. Кликните на вкладку **"Variables"**

**Вариант B: Через меню настроек**
1. В правом верхнем углу найдите три точки "..." или иконку настроек
2. Кликните на них
3. В выпадающем меню выберите **"Variables"**

#### Шаг 3: Добавьте первую переменную - BOT_TOKEN

1. В разделе "Variables" вы увидите форму:
   - Поле **"New Variable"** или **"Key"** (слева)
   - Поле **"Value"** (справа)
   - Кнопка **"Add"** или **"+"**

2. В поле **"Key"** введите:
   ```
   BOT_TOKEN
   ```

3. В поле **"Value"** введите:
   ```
   8525736564:AAH8SsvyBua9zbrK9xim5M6P25f-pvxohAw
   ```

4. Нажмите кнопку **"Add"** или **"+"**

5. Переменная появится в списке ниже

#### Шаг 4: Добавьте вторую переменную - ADMIN_USER_ID

1. Снова заполните форму:
   - **Key**: `ADMIN_USER_ID`
   - **Value**: `987445087`
   - Нажмите **"Add"**

#### Шаг 5: Добавьте третью переменную - DB_PATH (опционально)

1. Заполните форму:
   - **Key**: `DB_PATH`
   - **Value**: `secret_santa.db`
   - Нажмите **"Add"**

#### Шаг 6: Проверьте результат

После добавления всех переменных вы должны увидеть таблицу:

| Key | Value |
|-----|-------|
| BOT_TOKEN | `8525736564:AAH8SsvyBua9zbrK9xim5M6P25f-pvxohAw` |
| ADMIN_USER_ID | `987445087` |
| DB_PATH | `secret_santa.db` |

**Важно:** Значения могут быть скрыты точками (•••) для безопасности - это нормально.

#### Шаг 7: Перезапуск деплоя

После добавления переменных Railway обычно автоматически перезапускает деплой. Если этого не произошло:

1. Перейдите на вкладку **"Deployments"**
2. Найдите последний деплой
3. Кликните на три точки "..." рядом с деплоем
4. Выберите **"Redeploy"**

Или:

1. В настройках сервиса найдите кнопку **"Redeploy"**
2. Кликните на неё

---

### Способ 2: Через Railway CLI

Если у вас установлен Railway CLI, вы можете добавить переменные через командную строку.

#### Установка Railway CLI

```bash
# macOS
brew install railway

# Или через npm
npm i -g @railway/cli
```

#### Авторизация

```bash
railway login
```

#### Подключение к проекту

```bash
railway link
# Выберите ваш проект из списка
```

#### Добавление переменных

```bash
# Добавить BOT_TOKEN
railway variables set BOT_TOKEN=8525736564:AAH8SsvyBua9zbrK9xim5M6P25f-pvxohAw

# Добавить ADMIN_USER_ID
railway variables set ADMIN_USER_ID=987445087

# Добавить DB_PATH
railway variables set DB_PATH=secret_santa.db
```

#### Проверка переменных

```bash
railway variables
```

Вы должны увидеть список всех переменных окружения.

---

## Проверка работы бота

После успешного деплоя:

1. Перейдите на вкладку **"Deployments"**
2. Убедитесь, что последний деплой имеет статус **"Success"** (зелёная галочка)
3. Перейдите на вкладку **"Metrics"** или **"Logs"** чтобы увидеть логи бота
4. В логах должна быть строка: `Бот запущен...`

5. Откройте Telegram и найдите вашего бота: `@secret_santa43_bot`
6. Отправьте команду `/start` - бот должен ответить

---

## Устранение проблем

### Бот не отвечает

1. Проверьте логи в Railway (вкладка "Logs")
2. Убедитесь, что все переменные окружения добавлены правильно
3. Проверьте, что токен бота правильный (можно проверить через BotFather: `/token`)

### Ошибка "BOT_TOKEN не установлен"

1. Убедитесь, что переменная `BOT_TOKEN` добавлена в Railway
2. Проверьте, что название переменной написано точно: `BOT_TOKEN` (без пробелов, с заглавными буквами)
3. Перезапустите деплой

### Ошибка "ADMIN_USER_ID не установлен"

1. Убедитесь, что переменная `ADMIN_USER_ID` добавлена
2. Проверьте, что значение - это число без кавычек: `987445087`

### База данных не сохраняется

Railway использует эфемерную файловую систему. Для постоянного хранения данных рассмотрите использование Railway PostgreSQL или другого сервиса базы данных.

---

## Дополнительные настройки

### Настройка домена

1. В настройках проекта перейдите в **"Settings"**
2. Найдите раздел **"Domains"**
3. Railway автоматически предоставит домен для вашего проекта

### Мониторинг и логи

- **Logs**: Просмотр логов в реальном времени
- **Metrics**: Мониторинг использования ресурсов (CPU, память)
- **Deployments**: История всех деплоев

---

## Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Telegram Bot API](https://core.telegram.org/bots/api)

