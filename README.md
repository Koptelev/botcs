# 🎄 Тайный Санта - Telegram бот

Telegram-бот для организации игры «Тайный Санта» на Python с использованием библиотеки `python-telegram-bot`.

## 📋 Описание

Бот позволяет участникам регистрироваться в игре «Тайный Санта», указывая своё ФИО и желаемый подарок. Администратор может запустить случайное распределение участников, после чего каждый получит информацию о том, кому нужно подарить подарок.

## 🚀 Функциональность

- ✅ Регистрация участников с указанием ФИО и желаемого подарка
- ✅ Описание активности и правил игры
- ✅ Интерактивное меню помощи с кнопками для быстрого доступа к командам
- ✅ Административная функция распределения участников
- ✅ Выгрузка таблицы участников с детализацией (для администратора)
- ✅ Защита от повторной регистрации и повторного распределения
- ✅ Сохранение данных в SQLite базе данных

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

## ⚙️ Настройка

1. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

2. Откройте `.env` и заполните необходимые переменные:
```env
BOT_TOKEN=your_bot_token_here
ADMIN_USER_ID=your_telegram_user_id_here
DB_PATH=secret_santa.db
```

### Как получить BOT_TOKEN:
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен в `.env`

### Как получить ADMIN_USER_ID:
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш ID в `.env`

## 🏃 Запуск локально

```bash
python bot.py
```

Бот будет работать в режиме long polling и готов к использованию.

## 🚂 Развёртывание на Railway

### Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub
2. Загрузите код в репозиторий:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin your_repo_url
git push -u origin main
```

### Шаг 2: Создание проекта на Railway

1. Перейдите на [Railway](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий

### Шаг 3: Настройка переменных окружения

После того, как Railway подключил ваш репозиторий и начал деплой, нужно добавить переменные окружения:

#### 3.1. Откройте настройки проекта

1. В интерфейсе Railway найдите ваш проект
2. Нажмите на проект, чтобы открыть его дашборд
3. В верхней части экрана найдите вкладку **"Variables"** (или кликните на три точки "..." рядом с названием проекта и выберите **"Variables"**)

#### 3.2. Добавьте переменные окружения

В разделе "Variables" вы увидите форму для добавления переменных. Добавьте каждую переменную по отдельности:

**Переменная 1: BOT_TOKEN**
- В поле **"Key"** (или "Название") введите: `BOT_TOKEN`
- В поле **"Value"** (или "Значение") введите: `8525736564:AAH8SsvyBua9zbrK9xim5M6P25f-pvxohAw`
- Нажмите кнопку **"Add"** (или "Добавить")

**Переменная 2: ADMIN_USER_ID**
- В поле **"Key"** введите: `ADMIN_USER_ID`
- В поле **"Value"** введите: `987445087`
- Нажмите кнопку **"Add"**

**Переменная 3: DB_PATH** (опционально)
- В поле **"Key"** введите: `DB_PATH`
- В поле **"Value"** введите: `secret_santa.db`
- Нажмите кнопку **"Add"`

#### 3.3. Проверка переменных

После добавления всех переменных вы должны увидеть список:
```
BOT_TOKEN = 8525736564:AAH8SsvyBua9zbrK9xim5M6P25f-pvxohAw
ADMIN_USER_ID = 987445087
DB_PATH = secret_santa.db
```

#### 3.4. Перезапуск деплоя

После добавления переменных окружения:
1. Railway автоматически перезапустит деплой с новыми переменными
2. Или вы можете вручную перезапустить деплой: нажмите на три точки "..." рядом с сервисом → **"Redeploy"**

#### Альтернативный способ (через CLI)

Если у вас установлен Railway CLI, вы можете добавить переменные через командную строку:

```bash
railway variables set BOT_TOKEN=8525736564:AAH8SsvyBua9zbrK9xim5M6P25f-pvxohAw
railway variables set ADMIN_USER_ID=987445087
railway variables set DB_PATH=secret_santa.db
```

### Шаг 4: Настройка процесса

Railway автоматически определит `Procfile` и запустит бота как worker процесс.

### Шаг 5: Деплой

Railway автоматически задеплоит ваш проект. После успешного деплоя бот будет работать 24/7.

## 📱 Команды бота

### Для всех пользователей:
- `/start` - Начать работу с ботом
- `/about` - Описание игры и правил
- `/register` - Зарегистрироваться в игре
- `/status` - Показать статус регистрации
- `/help` или `/menu` - Показать меню с доступными командами (с интерактивными кнопками)
- `/cancel` - Отменить текущую регистрацию

### Для администратора:
- `/assign` - Запустить распределение участников
- `/export` - Выгрузить таблицу участников с детализацией (ФИО, желаемые подарки, распределение пар)
- `/status` - Показать общий статус игры и список участников
- `/reset_assignments` - Сбросить распределение (начать заново, участники остаются)
- `/reset` - Полный сброс (удалить всех участников, распределения и настройки)

## 🔒 Безопасность

- Только администратор может запускать распределение
- Участник может зарегистрироваться только один раз (повторная регистрация обновляет данные)
- Распределение можно выполнить только один раз
- Данные хранятся в локальной SQLite базе данных

## 📝 Структура проекта

```
botcs/
├── bot.py              # Основной файл бота
├── database.py         # Работа с базой данных
├── config.py           # Загрузка конфигурации
├── requirements.txt    # Зависимости Python
├── Procfile            # Конфигурация для Railway
├── .env.example        # Пример файла с переменными окружения
├── .gitignore          # Игнорируемые файлы
└── README.md           # Документация
```

## 🛠 Технологии

- Python 3.8+
- python-telegram-bot 20.7
- SQLite
- python-dotenv

## 📄 Лицензия

Этот проект создан для образовательных целей.

